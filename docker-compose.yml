version: "3.5"
services:
  php-apache:
    container_name: ${APP_CONTAINER_NAME}
    build:
      context: .
      dockerfile: ./docker-php-apache/Dockerfile
    ports:
      - 8080:80
      - 8443:443
    env_file:
      - ./docker-php-apache/php-apache.env
    environment:
      CRLIBRE_API_HACIENDA_DB_HOST: ${DB_HOST}
      CRLIBRE_API_HACIENDA_DB_NAME: ${DB_NAME}
      CRLIBRE_API_HACIENDA_DB_USER: ${DB_USER}
      CRLIBRE_API_HACIENDA_DB_PASSWORD: ${DB_PASSWORD}
    depends_on:
      - "mariadb"
    networks:
      - crlibrenet
    volumes:
      - ./www/settings.php:/var/www/html/settings.php
      - ./api:/var/www/html/api
  mariadb:
    image: mariadb:10.11.4
    container_name: ${MARIADB_CONTAINER_NAME}
    ports:
      - 4407:3306
    env_file:
      - ./docker-php-apache/php-apache.env
    volumes:
      - ./recursos/sql:/docker-entrypoint-initdb.d
      - ./mysql-data:/var/lib/mysql
    environment:
      TZ: "America/Costa_Rica"
      MYSQL_ALLOW_EMPTY_PASSWORD: "no"
      MYSQL_ROOT_PASSWORD: ${DB_ROOT_PASSWORD}
      MYSQL_USER: ${DB_USER}
      MYSQL_PASSWORD: ${DB_PASSWORD}
      MYSQL_DATABASE: ${DB_NAME}
    restart: always
    tty: true
    networks:
      - crlibrenet

networks:
  crlibrenet:
    driver: bridge
    driver_opts:
      com.docker.network.enable_ipv6: "false"
